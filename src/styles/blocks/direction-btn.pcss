.direction-btn {
  display: block;
  position: relative;
}

.direction-btn__icon {
  width: 8px;
  height: 10px;
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
}

.direction-btn_prev {
  transform: rotate(180deg);
}